# Database (로컬 Docker 기준)
DATABASE_URL="postgresql://princess:princess@localhost:5432/princessgram?schema=public"

# Auth.js — openssl rand -base64 32 로 생성한 값
AUTH_SECRET=""

# 사용자별 OpenAI API 키 암호화용 (32 bytes base64). `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` 로 생성
APP_ENCRYPTION_KEY=""
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Resend
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Princessgram <onboarding@resend.dev>"

# App
APP_BASE_URL="http://localhost:3000"

# Admin (쉼표로 여러 이메일 구분). 미설정 시 관리자 페이지 접근 불가
ADMIN_EMAILS=""

# (선택: 추후 기능용)
OPENAI_API_KEY=""
UPLOADTHING_SECRET=""
UPLOADTHING_APP_ID=""
